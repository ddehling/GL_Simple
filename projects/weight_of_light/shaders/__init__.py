"""Weight of Light project-local shader effects.

Shaders authored specifically for the Weight of Light installation live
here. The shared shader library at ``renderer/effects/`` is still
importable; the WoL event_map can mix shared + project-local effects.

Today this directory is empty. WoL-specific shaders (per-object pulse,
sensor-reactive bloom, etc.) belong here when authored.
"""
